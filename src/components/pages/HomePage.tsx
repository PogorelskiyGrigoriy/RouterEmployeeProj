import { Box, Text } from "@chakra-ui/react";

const HomePage = () => {
    return (
        <Box>
            <Text fontSize="3rem">HomePage</Text>
        </Box>
    );
};

export default HomePage;
